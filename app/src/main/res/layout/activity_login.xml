<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="32dp">

        <!-- Logo/Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="MobileCLI"
            android:textSize="36sp"
            android:textColor="#6750A4"
            android:textStyle="bold"
            android:layout_marginTop="48dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pro"
            android:textSize="24sp"
            android:textColor="#10A37F"
            android:textStyle="bold"
            android:layout_marginBottom="8dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="AI-Powered Mobile Terminal"
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginBottom="48dp" />

        <!-- Email Input (hidden: Google OAuth only for now) -->
        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:visibility="gone"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">

            <EditText
                android:id="@+id/email_input"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Email"
                android:inputType="textEmailAddress"
                android:textColor="#1C1B1F"
                android:textColorHint="#666666" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- Password Input (hidden: Google OAuth only for now) -->
        <com.google.android.material.textfield.TextInputLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp"
            android:visibility="gone"
            style="@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox">

            <EditText
                android:id="@+id/password_input"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Password"
                android:inputType="textPassword"
                android:textColor="#1C1B1F"
                android:textColorHint="#666666" />

        </com.google.android.material.textfield.TextInputLayout>

        <!-- Error Text -->
        <TextView
            android:id="@+id/error_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#B3261E"
            android:textSize="14sp"
            android:visibility="gone"
            android:layout_marginBottom="16dp" />

        <!-- Login Button (hidden: Google OAuth only for now) -->
        <Button
            android:id="@+id/login_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Log In"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#6750A4"
            android:layout_marginBottom="12dp"
            android:visibility="gone" />

        <!-- Signup Button (hidden: Google OAuth only for now) -->
        <Button
            android:id="@+id/signup_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Create Account"
            android:textSize="16sp"
            android:textColor="#6750A4"
            android:backgroundTint="#E8DEF8"
            android:layout_marginBottom="24dp"
            android:visibility="gone" />

        <!-- Divider (hidden: no email/password to divide from) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="24dp"
            android:visibility="gone">

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1"
                android:background="#E0E0E0" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="  or continue with  "
                android:textSize="12sp"
                android:textColor="#666666" />

            <View
                android:layout_width="0dp"
                android:layout_height="1dp"
                android:layout_weight="1"
                android:background="#E0E0E0" />

        </LinearLayout>

        <!-- Sign in with label -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sign in to continue"
            android:textSize="16sp"
            android:textColor="#49454F"
            android:layout_marginBottom="24dp" />

        <!-- Google Sign In Button -->
        <Button
            android:id="@+id/google_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Continue with Google"
            android:textSize="16sp"
            android:textColor="#1C1B1F"
            android:backgroundTint="#F5F5F5"
            android:layout_marginBottom="12dp" />

        <!-- GitHub Sign In Button -->
        <Button
            android:id="@+id/github_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Continue with GitHub"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#24292e"
            android:layout_marginBottom="12dp" />

        <!-- Apple Sign In Button (hidden: not configured yet) -->
        <Button
            android:id="@+id/apple_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Continue with Apple"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#000000"
            android:layout_marginBottom="12dp"
            android:visibility="gone" />

        <!-- Microsoft Sign In Button (hidden: not configured yet) -->
        <Button
            android:id="@+id/microsoft_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Continue with Microsoft"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#0078D4"
            android:layout_marginBottom="12dp"
            android:visibility="gone" />

        <!-- Discord Sign In Button (hidden: not configured yet) -->
        <Button
            android:id="@+id/discord_button"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Continue with Discord"
            android:textSize="16sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#5865F2"
            android:layout_marginBottom="32dp"
            android:visibility="gone" />

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:indeterminateTint="#6750A4" />

        <!-- Terms notice -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="By continuing, you agree to our Terms of Service and Privacy Policy"
            android:textSize="12sp"
            android:textColor="#999999"
            android:gravity="center"
            android:layout_marginTop="16dp" />

        <!-- Skip login REMOVED for production - security risk -->
        <TextView
            android:id="@+id/skip_login"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text=""
            android:textSize="14sp"
            android:textColor="#6750A4"
            android:padding="0dp"
            android:visibility="gone" />

    </LinearLayout>

</ScrollView>
